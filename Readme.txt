Specify array file with the argument:
-a [filepath]

Specify graph file with the argument:
-g [filepath]

To write a newly generated file at the provided location, add the arguement:
-wg

Similarly, to write a newly generated array, use:
-wa

If no path is specified, the program will simply generate an object to use.